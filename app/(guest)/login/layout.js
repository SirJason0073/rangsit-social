export const metadata = {
  title: 'Log in',
  description: 'Log in to Rangsit Social.',
  robots: { index: false, follow: true }
};

export default function LoginLayout({ children }) {
  return children;
}
